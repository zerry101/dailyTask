import { DirstyleDirective } from './dirstyle.directive';

describe('DirstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirstyleDirective();
    expect(directive).toBeTruthy();
  });
});
